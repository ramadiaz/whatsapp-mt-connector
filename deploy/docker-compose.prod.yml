version: "3.9"

services:
  whatsapp-mt-connector:
    image: whatsapp-mt-connector:${APP_VERSION:-latest}
    restart: unless-stopped
    env_file: ../.env
    ports:
      - "8080:8080"
    networks:
      - internal

networks:
  internal:
    driver: bridge
